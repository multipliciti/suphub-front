'use client';
import s from './PasswordChanged.module.scss';

export const PasswordChanged = () => {

	return (
		<div className={s.wrapper}>
			Password is successfully changed! You're welcome!
		</div>
	);
};
